<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_approval_comment_wizard_form" model="ir.ui.view">
        <field name="name">ml.approval.comment.wizard.form</field>
        <field name="model">ml.approval.comment.wizard</field>
        <field name="arch" type="xml">
            <form string="Add Comments">
                <sheet>
                    <group>
                        <group>
                            <field name="purchase_order_id" invisible="1"/>
                            <field name="approval_level_id" invisible="1"/>
                            <field name="comment" placeholder="Enter your comments here..." nolabel="1"/>
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button name="action_confirm_with_comment" string="Confirm" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_approval_comment_wizard" model="ir.actions.act_window">
        <field name="name">Approval Comments</field>
        <field name="res_model">ml.approval.comment.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="purchase.model_purchase_order"/>
        <field name="binding_view_types">form</field>
    </record>
</odoo>